rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allows public submission creation, but completely locks down access to existing data
    match /{document=**} {
      allow create: if true;
      allow read, update, delete: if false;
    }
  }
}
